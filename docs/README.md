# docs

This folder contains the presentation of the current project.

## Structure

```text
docs/
|-- pages/
```

### `pages/`

Static HTML documentation pages with shared assets, navigation, and screenshots.

## Purpose

The `docs/` folder explains the problem, the modeling approach, and the example implementations without being tied to the runtime code itself.
